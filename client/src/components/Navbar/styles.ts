import styled from "styled-components";

export const Header = styled.header`
  position: fixed;
  top: 0;
  width: 100%;
  height: 100px;
  background-color: var(--bg-color);
  box-shadow: 0px 10px 10px 1px rgb(0 0 0 / 50%);
`;

export const Navigation = styled.nav``;
